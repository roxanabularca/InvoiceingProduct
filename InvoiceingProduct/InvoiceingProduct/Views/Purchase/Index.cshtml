@model IEnumerable<InvoiceingProduct.Models.PurchaseModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 style="font-weight:bold;color:red">Purchase</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@if (Model !=null)
{
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.IdPurchase)
            </th>*@
           @* <th>
                @Html.DisplayNameFor(model => model.IdOffer)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.PurchaseName)
            </th>
             <th>
                @Html.DisplayNameFor(model => model.OfferName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.IdPurchase)
            </td>*@
           @* <td>
                @Html.DisplayFor(modelItem => item.IdOffer)
            </td>*@
              <td>
                @Html.DisplayFor(modelItem => item.PurchaseName)
            </td>
              <td>
                @Html.DisplayFor(modelItem => item.OfferName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.IdPurchase }) |
                @Html.ActionLink("Details", "Details", new {  id=item.IdPurchase }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.IdPurchase  })
            </td>
        </tr>
}
    </tbody>
</table>
}
else
{
    <p>"empty model"</p>
}